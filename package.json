{
  "name": "comedian-website",
  "private": true,
  "description": "Static site — npm run dev serves on port 3333 (avoid 3000 if another app uses it). Use vercel dev for APIs.",
  "scripts": {
    "dev": "serve . -p 3333",
    "start": "serve . -p 3333"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
